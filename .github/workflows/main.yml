name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          # تمكين سطح المكتب البعيد وتعطيل مصادقة مستوى الشبكة
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          # إزالة أي قاعدة حماية موجودة بنفس الاسم
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          
          # السماح بأي اتصال وارد على منفذ 3389
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389

          # إعادة تشغيل خدمة سطح المكتب البعيد
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Static Credentials
        run: |
          # بيانات الاعتماد الثابتة
          $username = "RDP"
          $password = "Yaso1212" # يرجى تغيير هذه كلمة المرور لشيء أكثر أماناً
          
          # تحويل كلمة المرور إلى SecureString
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          # إنشاء المستخدم إذا لم يكن موجوداً
          if (-not (Get-LocalUser -Name $username -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name $username -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member $username
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
              Write-Host "تم إنشاء المستخدم الجديد: $username"
          } else {
              # تحديث كلمة مرور المستخدم الموجود
              Set-LocalUser -Name $username -Password $securePass
              Write-Host "تم تحديث كلمة مرور المستخدم الموجود: $username"
          }
          
          # تخزين بيانات الاعتماد في متغيرات البيئة
          echo "RDP_USERNAME=$username" >> $env:GITHUB_ENV
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          # تشغيل Tailscale بمفتاح المصادقة
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          # الانتظار حتى يتم تعيين IP
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "لم يتم تعيين عنوان IP من Tailscale. الخروج."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Verify RDP Accessibility
        run: |
          Write-Host "عنوان Tailscale IP: $env:TAILSCALE_IP"
          
          # اختبار الاتصال على منفذ 3389
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "فشل الاتصال TCP على منفذ RDP 3389"
              exit 1
          }
          Write-Host "تم الاتصال TCP بنجاح!"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== الوصول عبر RDP ==="
          Write-Host "العنوان: $env:TAILSCALE_IP"
          Write-Host "اسم المستخدم: $env:RDP_USERNAME"
          Write-Host "كلمة المرور: $env:RDP_PASSWORD"
          Write-Host "==================`n"
          
          # إبقاء العدّاد نشطاً
          while ($true) {
              Write-Host "[$(Get-Date)] RDP نشط - استخدم Ctrl+C في سير العمل للإنهاء"
              Start-Sleep -Seconds 300
          }
